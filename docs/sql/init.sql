-- Baseline SQL for BUMAS Ansor (PostgreSQL)
-- Source of truth tetap di Prisma schema.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Apply actual schema using:
-- npx prisma migrate dev --name init
